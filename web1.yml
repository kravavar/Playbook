---
 - hosts: all
   remote_user: root
   become: yes
   tasks:
    - name: sruthi sruthi sruthi install the httod
      yum: name=httpd state=present
    - name: services add to the httpd service restart
      service: name=httpd state=started
